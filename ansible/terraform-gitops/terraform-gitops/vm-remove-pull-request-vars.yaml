---
# VM Removal specific configuration
new_branch_name: "feature/{{ req_id }}-remove-vm"  # Name for your new branch with request ID
commit_message: "feat: Remove VM {{ instance_name_to_remove }} per {{ req_id }}"  # Commit message with request ID

# Pull Request Configuration
pr_title: "feat: Remove VM {{ instance_name_to_remove }} per {{ req_id }}"  # Title for your pull request with request ID
pr_body: >-
  This pull request was automatically created by an Ansible playbook to remove the EC2 instance
  '{{ instance_name_to_remove }}' for request {{ req_id }}.

  **Instance being removed:** {{ instance_name_to_remove }}
  **Request ID:** {{ req_id }}

  Please review the changes before merging.

# VM Removal Configuration
# instance_name_to_remove: ""  # REQUIRED: Name of the VM instance to remove (should match the name used in the Terraform block)
